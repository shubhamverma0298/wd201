<!DOCTYPE html>
<html lang="en"> 
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To-Do Manager</title>
    <link rel="stylesheet" href="./css/styles.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <!-- <h1 style="text-align: center;">Todo Home page</h1> -->
    <div class="grid grid-cols-4">
        <div class="col-start-2 col-span-1">
            <%- include('header.ejs') %>
            <form action="">
                <div class="flex gap-2 py-4 ">
                    <div class="flex-auto">
                        <input type="text" placeholder="what's next? " class="border border-grey-300 rounded text-grey-900 w-full p-2 text-sm "/>
                    </div>
                </div>
                <div class="flex-auto">
                    <input type="date" class="border border-grey-300 rounded text-grey-900 w-full p-2 text-sm  leading-4"/>
                </div>
                </div>
                <div class="flex-none">
                <button type="submit" class="bg-green-600 text-white px-5 px-2 rounded py-1 font-medium mr-2 mb-2"> Add</button>
                </div>
            </form>
        <div>
            <h5>Overdue</h5>
            <span id="count-overdue">
                <%= overdue.length %>
            </span> <!-- Count of overdue todos -->
            <%- include('todos.ejs', { title: "Overdue" , data: overdue }) %>
    
            <h5>Due Today</h5>
            <span id="count-due-today">
                <%= dueToday.length %>
            </span> <!-- Count of due today todos -->
            <%- include('todos.ejs', { title: "Due Today" , data: dueToday }) %>
    
            <h5>Due Later</h5>
            <span id="count-due-later">
                <%= dueLater.length %>
            </span> <!-- Count of due later todos -->
            <%- include('todos.ejs', { title: "Due Later" , data: dueLater }) %>
        </div>
        </div>
    </div>
    <%- include('footer.ejs') %> 
</body>
</html>